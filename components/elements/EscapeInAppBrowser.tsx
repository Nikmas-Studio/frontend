'use client';

import { useEscapeInAppBrowser } from '@/hooks/use-escape-in-app-browser';

function EscapeInAppBrowser(): null {
  useEscapeInAppBrowser();

  return null;
}

export default EscapeInAppBrowser;
